# Agent policy

- Bash(rm *): deleting repository files has a large blast radius and must remain a student-controlled action.
- Bash(git push *): publishing repository changes affects submitted history and requires explicit student approval.
- Bash(docker *): Docker operations can remove containers or persistent volumes and therefore require student control.
- WebFetch: external network access is unnecessary for reviewing the local course specification and increases agent scope.
